class Project {
  const Project(
      {this.id, this.title, this.slug, this.scrollLength, this.banner});

  final int id;
  final String title;
  final String slug;
  final double scrollLength;
  final String banner;
}
